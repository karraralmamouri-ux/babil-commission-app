# نشر RPC المالي الذري — 2026-08-04

## النطاق

طُبقت المهاجرة `20260804230000_add_atomic_financial_rpcs.sql` على Staging ثم الإنتاج. لا تربط هذه المهاجرة الواجهة بالبيانات المركزية ولا ترحّل `localStorage`؛ تضيف مسارات الكتابة المراجعة وتقفل الكتابة المباشرة استعداداً للربط التدريجي.

## تحقق Staging

- نسخة custom dump قبل التغيير: 36,244 بايت، 105 عناصر TOC و6 عناصر TABLE DATA.
- SHA-256: `13a7cbccf967b7ed0490327910fc5e64147a76d287e9f85f6ed6569a7d34b347`.
- نجحت 56 مطالبة Auth/RLS/RPC للأدوار الأربعة.
- تحقق replay لنفس `request_id`، رفض التعارض، منع الدفع فوق المستحق، ومنع خفض المستحق تحت المدفوع.
- بعد التنظيف: صفر أشهر fixture وصفر audit للـfixture.

## نسخة الإنتاج قبل التغيير

لأن الخطة المجانية لا توفر scheduled backup ولأن Docker غير متوفر، نُفذ الإجراء الانتقالي ADR-008: تصدير جداول `public` الستة عبر REST بصلاحية خادم مؤقتة إلى مجلد محلي مستبعد من Git.

| الجدول | الصفوف | SHA-256 |
|---|---:|---|
| `profiles` | 2 | `a34544b4ff1afc0f03743728ed215303d202e1dea9ced10f9bca20dd24d604ac` |
| `commission_months` | 2 | `8890fac20b6f7ac12824589038d4bb4371842fad2b12158c52e951bcd9b18bca` |
| `commission_rows` | 82 | `fd84d5b2623c07da9c3176a03492be2548b1bdaed4d2421f9f715fa8e2fd93b4` |
| `commission_agents` | 0 | `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945` |
| `audit_logs` | 0 | `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945` |
| `commission_audit_logs` | 0 | `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945` |

النسخة المحلية: `backups/production-pre-atomic-2026-08-04/`، وهي مستبعدة من Git ومقيدة الوصول.

## إصلاح سجل migrations

كان مخطط الإنتاج يحتوي migrations الثلاث السابقة فعلياً لكن جدول تاريخ Supabase لم يسجلها. أظهر dry-run محاولة إعادة baseline، فتوقف النشر قبل أي SQL. سُجلت الإصدارات التالية كـ`applied` دون إعادة تنفيذها:

- `20260804170000`
- `20260804183000`
- `20260804203000`

بعد الإصلاح عرض dry-run ملف `20260804230000` وحده، ثم طُبق. أظهر dry-run النهائي أن قاعدة الإنتاج `up-to-date`.

## تحقق ما بعد النشر

- تطابقت SHA-256 لكل ملفات الجداول الستة قبل وبعد المهاجرة.
- بقيت الأعداد: profileان، فترتان، 82 صف عمولة، وصفر في الجداول الثلاثة الفارغة.
- استعلام REST المحدد لحقول `entity_type`, `entity_id`, `before_data`, `after_data`, `request_id` أعاد HTTP 200.
- GitHub CI وPages على commit `7bb4395` ناجحان، وصفحة الإنتاج HTTP 200.
- لا توجد كتابة بيانات مالية أو ترحيل `localStorage` ضمن هذا النشر.

## الخطوة التالية

1. تصدير وفحص نسخة JSON من كل متصفح تشغيل.
2. مطابقة المجاميع بين النسخ المحلية والصفوف المركزية دون استبدال أي مصدر.
3. بناء ربط قراءة مركزي خلف pilot، ثم تفعيل RPC الكتابة لمستخدمين محددين بعد قبول المطابقة.
