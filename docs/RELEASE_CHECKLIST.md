# قائمة الإصدار — تفاصيل الكابينات وأجور التنصيب

مرشح إصدار من فرع `feat/installation-fees` فوق `main@3c03502`.

**الحالة: لم يُنشر شيء.** لم تُطبَّق أي مهاجرة على الإنتاج، ولم يُنشر التطبيق، ولم تُمسّ مصادقة الإنتاج ولا بياناته.

## ما تم التحقق منه قبل هذه القائمة

| التحقق | النتيجة |
|---|---|
| `npm test` | 111 / 111 |
| `npm run localdb:test` | 59 / 59 على Postgres محلي |
| تحقق قاعدة Staging | 59 / 59 على `unohqhxubraelqgjhxgh` |
| تحقق المتصفح على Staging | 26 سيناريو، بدورَي admin وaccountant |
| نظافة Staging بعد الاختبار | أعداد ما بعد الاختبار = أعداد ما قبله |
| الإنتاج | لم يُمسّ |

## قبل الإنتاج

- [ ] مراجعة الـPR
- [ ] CI أخضر على الفرع
- [ ] مراجعة الـdiff النهائي من `3c03502`
- [ ] تأكيد خطة النسخ الاحتياطي والاسترجاع للإنتاج
- [ ] فحص سجل مهاجرات الإنتاج: هل `20260815113000` مطبَّقة فعلاً؟
- [ ] التحقق من الوصول الإداري إلى Supabase الإنتاجي
- [ ] التحقق من دخول admin إلى التطبيق الإنتاجي
- [ ] الحصول على موافقة صريحة على تطبيق المهاجرة

## قاعدة بيانات الإنتاج

- [ ] تأكيد حالة `20260815113000` — **لا تُعد تطبيقها إن كانت مطبَّقة**
- [ ] تطبيق `20260815160000_add_installation_fees.sql`
- [ ] تطبيق `20260815180000_tighten_installation_grants.sql`
- [ ] التحقق أن `authenticated` لا يملك `TRUNCATE` على أي جدول تنصيب
- [ ] التحقق من المخطط: 3 جداول، 6 دوال، 3 سياسات، 2 trigger، 30 قيداً
- [ ] التحقق من RLS والـRPCs بمصفوفة الأدوار الأربعة
- [ ] فحص سريع لأجور التنصيب على بيانات اختبار ثم حذفها

## إصدار التطبيق

- [ ] دمج الـPR
- [ ] نشر التطبيق
- [ ] التحقق من مسارات الأصول المنشورة، خصوصاً `assets/js/installation-fees.js`
- [ ] التحقق من تسجيل الدخول
- [ ] التحقق من الزون القديم
- [ ] التحقق من الزون الجديد وتفاصيل الكابينات
- [ ] التحقق من قسم أجور التنصيب

## بعد الإصدار

- [ ] مراجعة سجل التدقيق
- [ ] لا أخطاء صلاحيات غير متوقعة
- [ ] لا دفعات مكررة
- [ ] لا أخطاء استيراد
- [ ] متابعة أول استيراد وأول صرف حقيقيين

## الرجوع

مهاجرة الإنتاج forward-only. المهاجرة إضافية بالكامل ولا تلمس جداول العمولات، فأثرها على النظام القائم صفر إن لم تُستخدم الواجهة الجديدة. أي تصحيح لاحق يكون بمهاجرة forward جديدة لا بتراجع.

## خارج النطاق عمداً

إلغاء الدفعة وتصحيحها · تدقيق `admin-users` · تطبيق `is_visible` في RLS · فحص حالة الشهر في RPCs العمولات.
