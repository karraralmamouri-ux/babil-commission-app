{
  "name": "babil-commission-app",
  "version": "0.1.0",
  "private": true,
  "description": "Characterization tests for the Babil Commission App baseline.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:staging": "node scripts/verify-supabase-staging.mjs",
    "test:staging-admin": "node scripts/verify-admin-users-staging.mjs",
    "test:staging-breakdown": "node scripts/verify-source-breakdown-staging.mjs",
    "inspect:backup": "node scripts/inspect-local-backup.mjs",
    "reconcile:backup": "node scripts/reconcile-local-backup.mjs",
    "guard:project-ref": "node scripts/assert-project-ref.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "supabase": "^2.111.0"
  }
}
