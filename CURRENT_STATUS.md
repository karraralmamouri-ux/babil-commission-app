# الحالة الحالية — Babil Commission App

> هذا الملف يلخّص الواقع الحالي فقط. التاريخ التفصيلي موجود في `docs/PROJECT_STATUS.md` و`CHANGELOG.md`.

## الهوية

- النظام: إدارة عمولات وكلاء الإنترنت في بابل، بواجهة عربية RTL.
- المستودع البعيد: `karraralmamouri-ux/babil-commission-app`.
- النسخة المحلية المفحوصة: هذا المجلد على الفرع `feat/installation-monthly-calculator`، المتفرّع من `fix/post-deploy-qa-pack` عند `0269c3b` (رأس PR #95 المفتوح).
- العمل الحالي محلي بالكامل: لا نشر، لا دفع إلى البعيد، لا دمج.

## الحالة التنفيذية

- Supabase هو مصدر الحقيقة المستهدف؛ الواجهة منشورة عبر GitHub Pages.
- الصرف المركزي واعتماد الشهر واستمرار الجلسة وإدارة المستخدمين موثقة كمنفذة.
- بقية تعديلات العمولة القديمة ما زالت تحمل اعتماداً محلياً جزئياً بحسب `docs/PROJECT_STATUS.md`.
- نطاق أجور التنصيب منفذ ومختبر محلياً، لكن migration الخاصة به غير منشورة على Staging أو Production.
- **مسار الشهر الجديد لأجور التنصيب (ADR-034) منفَّذ ومختبَر محلياً بالكامل، وغير منشور.** ملفٌ شهريٌّ واحد يُغذّي محرّك العمولات ومحرّك التنصيب معاً؛ الأحداث تُعالَج زمنياً فيستهلك كلُّ حدثٍ صالحٍ المرحلةَ التالية؛ والحساب معاينةٌ لا تمسّ حالة مشترك، والاعتماد يُثبّت المراحل ولا يدفع. ثلاث migrations جديدة: `20261105090000` (تسلسل الوكيل/Print وحسم الأسماء) و`20261106090000` (المحرّك) و`20261107090000` (عقود القراءة)، وشاشة واحدة `src/features/installation/monthly.ts` على `/installation/monthly`.
- المسار السابق (دورة ← تدقيق فواتير بالجملة ← جاهز للصرف ← دفعة سداد ← دفتر) **باقٍ بحرفه ولم يُحذف منه شيء**؛ خرج من تعريف العمل الشهري فقط. راجع `CONTEXTUAL_ROUTES` في `src/app/shell.ts`.

## حالة Git المحلية

- توجد مجلدات غير متتبعة: `docs/audits/` و`scratch/` و`.claude/`. لا تحذفها ولا تستبدلها قبل فحص محتواها.
- الفرع `feat/installation-monthly-calculator` متفرّع من `0269c3b`، ولم يُدفَع إلى البعيد.
- PR #95 (`fix/post-deploy-qa-pack`) ما زال مفتوحاً وغير مدموج.
- بوّابات CI حُدِّثت محلياً: 104 migrations و1423 assertion في `.github/workflows/ci.yml`.

## مخاطر مفتوحة

- تعدد النسخ المحلية للمستودع قد يؤدي إلى العمل على نسخة قديمة؛ استخدم هذا المجلد كنسخة العمل الأساسية ما لم يُوثق نقل صريح.
- لا يجوز اختبار دفعة مالية وهمية على Production.
- لا تُنشر migrations أجور التنصيب قبل مراجعة الهوية والبيئة وخطة الرجوع.
- لا تُفترض قواعد تجارية ناقصة مثل الإلغاء والتصحيح والدفعات المتعددة.

## المراجع عند الحاجة

- السياق الثابت: `docs/PROJECT_CONTEXT.md`
- الأولويات: `TASKS.md`
- القرارات: `docs/DECISIONS.md`
- التاريخ التشغيلي: `docs/PROJECT_STATUS.md`
- خطة الانتقال: `docs/ROADMAP.md`
- سجل التغييرات: `CHANGELOG.md`
