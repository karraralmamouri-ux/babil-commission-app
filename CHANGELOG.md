# سجل التغييرات

يسجل هذا الملف التغييرات المهمة للمستخدم والتشغيل. تاريخ Git يبقى المصدر التفصيلي للتعديلات البرمجية.

## Unreleased

### Added

- إضافة توزيع هرمي تفصيلي يضع الوكيل الرئيسي في رأس المجموعة، ثم FDT، ثم حسابات parent المشاركة وتفعيلات وعمولة كل حساب، مع إجمالي مطابق للتقرير النهائي وتصدير CSV وExcel.
- تجهيز الإطلاق المركزي النظيف: تبقى الفترات القديمة محفوظة وقابلة للاسترجاع لكنها مخفية، ولا يظهر أي سجل حتى يعتمد الأدمن أول ملف جديد.
- إضافة إدارة أرشيف الفترات للأدمن مع عرض الفترة وفتحها للتعديل وتصديرها واستيرادها بصورة مستقلة.
- إضافة صيغة JSON خاصة بالفترة المختارة مع بصمة SHA-256، والتحقق من الشهر والكميات والتير والمدفوع قبل الاستيراد، مع الحفاظ على حقول الصرف المركزي الموجودة.
- إضافة اختيار الشهر يدوياً وفتح أي شهر مركزي محفوظ في وضع التجهيز لتعديله ثم إعادة اعتماده دون تغيير البيانات المشتركة قبل الاعتماد.
- إضافة اعتماد مركزي ذري للشهر يحفظ ربط الرئيسي والفروع وأساس تير المجموعة، ويتحقق من الأسعار والتير على الخادم ويحافظ على الدفعات السابقة.
- إضافة إعدادات مركزية لربط الوكلاء والكابينات، ومقارنات شهرية للصعود والنزول، وتقرير Excel بأوراق الملخص والعمولات والمقارنة والإعدادات.
- إضافة التبديل بين العربية والإنجليزية، وإتاحة إدارة المستخدمين والنسخة الاحتياطية للأدمن من مساحة البيانات المركزية.
- إزالة سجلات الوكلاء التجريبية المضمنة من التطبيق؛ يبدأ وضع التجهيز دون بيانات ثابتة ويعتمد على الملف الخام أو الأشهر المحفوظة.
- فصل حسابات الوكيل الرئيسي والفروع إلى سجلات مستقلة في OLD ZONE، مع تحديد التير تلقائياً من مجموع تفعيلات المجموعة وتطبيق سعره على كل سجل منفصل.
- تنظيف الواجهة الداكنة وإضافة سايدبار للتنقل والتحديث والتصدير وإجراءات الإدارة.
- إعادة تنظيم السايدبار إلى العرض والمتابعة، التقارير، تجهيز الشهر، وإدارة النظام، مع توضيح حالة المصدر ونتيجة كل إجراء.
- جعل البيانات المركزية مساحة العمل الافتراضية بعد تسجيل الدخول، مع مؤشر اتصال ووقت آخر تحديث وحالة واضحة عند تعذر الاتصال.
- نقل رفع الملف والأسعار والحفظ إلى «وضع تجهيز الشهر» المقيد بصلاحية المدير، وإزالة عرض تسمية النسخة المحلية من الاستخدام اليومي.
- إضافة استيراد مباشر لملف التفعيلات الخام مع عد `id` الفريد، واعتماد `parent`، وتصفية P-35000 وP-45000 وP-65000.
- إضافة ملف إعدادات مستقل ومحرر داخل الواجهة لحسابات الوكلاء ونطاقات كابينات الزون الجديد والتير الافتراضي.
- إضافة ملخص بعد الاستيراد يوضح الصفوف المقروءة والتفعيلات المؤهلة والتكرارات والباقات المستبعدة وحالات المراجعة.

- ربط صرف المدير والمحاسب بالعملية الذرية `record_commission_payment` من وضع البيانات المركزية، مع سجل تدقيق مشترك، منع التكرار، كشف التعارض، وتحديث يدوي للبيانات المشتركة.
- حفظ جلسة الدخول في المتصفح وتجديد access token تلقائياً باستخدام refresh token المدور، دون مسح الجلسة عند انقطاع شبكة مؤقت.
- إضافة أداة مطابقة محلية/مركزية للقراءة فقط، تتحقق من الأشهر والشرائح والصفوف والمجاميع وتكشف المحتوى المكرر دون طباعة أسماء الوكلاء أو السماح بالاستيراد التلقائي.
- إضافة تصدير نسخة احتياطية JSON إصدار 1 للمدير فقط، تشمل حالة التشغيل والأرشيف وسجل التعديلات وبصمة SHA-256، ولا تشمل جلسة Supabase أو رموز الدخول.
- استرجاع مصدري Edge Functions المنشورين إلى `supabase/functions` وإضافة Supabase CLI مثبت ومهاجرة أمن صلاحيات قابلة للمراجعة.
- إضافة baseline معاد البناء للمخطط الحالي وسكربت محمي لاختبار Auth وRLS على Staging.
- إضافة GitHub Actions CI لتثبيت التبعيات وتشغيل الاختبارات على Pull Requests و`main`.
- إضافة وتطبيق مهاجرة لعمليات تحديث العمولة وتسجيل الصرف الذرية مع سجل تدقيق منظم وكشف التعارض ومنع تكرار الطلب على Staging والإنتاج؛ الواجهة لم تربط بها بعد.
- إضافة أداة قراءة فقط لفحص نسخة المتصفح، والتحقق من بصمتها، وحساب مجاميع المطابقة دون عرض أسماء الوكلاء.

### Documentation

- إنشاء ذاكرة مشروع دائمة وقواعد مساهمة وتسليم.
- توثيق خط الأساس، البنية الحالية، المخاطر، الأمان، قاعدة البيانات المقترحة، الاختبارات وخارطة الطريق.
- إضافة نموذج متغيرات بيئة دون أسرار وقواعد تجاهل الملفات المحلية.

### Tests

- إضافة اختبارات توصيفية لمنطق العمولة وحدود الشرائح والدفع وCSV والاستيراد، تعمل مباشرة على JavaScript الحالي داخل `index.html`.
- إضافة اختبارات ساكنة تمنع تضمين مفاتيح Supabase وتثبت إعداد JWT وإغلاق تعديل profile الذاتي.
- إنشاء Staging مستقل وإعادة بنائه من migrations، والتحقق من تطابق المخطط ونجاح 48 اختبار صلاحيات للأدوار الأربعة و`admin-users`.
- إنشاء pg_dump حقيقي من Staging واختبار pg_restore، ثم نجاح 48 تحقق Auth/RLS بعد الاسترجاع.
- إضافة اختبارات تصميم لمسارات RPC المالية واختبارات تحقق ومطابقة لأداة جرد النسخ المحلية؛ أصبح الفحص المحلي 31 اختباراً ناجحاً.
- توسيع تحقق Staging إلى 56 مطالبة فعلية للأدوار الأربعة، تشمل منع الكتابة المباشرة وreplay والتعارض ومنع الدفع فوق المستحق وتنظيف fixture.
- مطابقة جداول الإنتاج الستة قبل وبعد مهاجرة RPC بالبصمة والعدد، وإصلاح تاريخ migrations السابق دون إعادة تنفيذ baseline.

### Security

- توثيق تدقيق Supabase الفعلي وإعداد إصلاح لمسار تصعيد الدور في `profiles` وتقليص منح `anon` و`authenticated` وجعل التدقيق append-only.
- تطبيق مهاجرة تحصين الصلاحيات على Supabase الإنتاجي والتحقق من policies وgrants ومنع تعديل profile المباشر بمحاكاة دوري admin وaccountant.
- حذف رمز الوصول المؤقت بعد اكتمال التدقيق والتحقق من عدم بقاء Access Tokens في الحساب.
- تطبيق قيود validated على Staging والإنتاج لمنع الكميات والمدفوعات السالبة مع لقطة بيانات وفحص مسبق.

### Fixed

- إصلاح إدارة المستخدمين للأدمن بإزالة تعارض تحقق JWT في بوابة Edge Function، مع إبقاء التحقق من رمز الجلسة والحساب النشط ودور `admin` داخل الخادم.
- إظهار رسالة الخطأ الحقيقية القادمة من خدمة الحسابات بدلاً من رسالة عامة، وإضافة سجل تدقيق لإنشاء الحساب وتغيير الدور أو الحالة وكلمة المرور من دون تخزين كلمة المرور أو بيانات الحساب الشخصية في السجل.
- منع جمع تغيير الصلاحية وكلمة المرور في طلب واحد حتى لا يحدث تنفيذ جزئي، وجعل إنشاء الحساب وتغيير الصلاحية يتراجعان عند فشل التدقيق، مع تسجيل طلب تغيير كلمة المرور قبل تنفيذه.

- استبدال مسار مكتبة Excel المفقود بنسخة محلية مثبتة من SheetJS CE 0.20.3، مع ترخيص واختبار قراءة وكتابة XLSX.
- ترتيب الأشهر والمقارنة مع آخر شهر مؤرشف يعملان زمنياً عبر نهاية السنة.
- إنشاء الشهر الجديد يتقدم من الشهر المفتوح بدلاً من تاريخ جهاز المستخدم.

### Changed

- أصبح الزون القديم يجمع الرئيسي مع فروعه لاختيار التير فقط، ثم يعرض ويحسب كل حساب رئيسي أو فرعي كسجل مستقل؛ الزون الجديد يبقى محسوباً لكل كابينة ويعتمد مالك نطاق FDT بغض النظر عن parent.
- يحافظ استيراد ملف جديد على التير اليدوي والمدفوع وتاريخ الصرف للسجلات الموجودة بنفس الاسم.

- أضيفت صلاحية مستقلة `backup` لمنع تصدير البيانات التشغيلية من أدوار غير المدير.
- حماية `main` بفحص CI إلزامي strict ومنع force-push والحذف مع linear history وحل المحادثات.

## Baseline — 2026-08-04

- خط الأساس الموثق: commit `05e51c631cef29ad93f8072c1221fc9493f5c24f`.
- التطبيق موجود في `index.html` ويُنشر عبر GitHub Pages.
