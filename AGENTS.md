# تعليمات العمل الدائمة

هذا المستودع مشروع مستمر وطويل الأمد لنظام مالي تشغيلي. الأولوية دائماً لصحة البيانات، قابلية الاسترجاع، الدقة المالية، والأمان قبل سرعة إضافة الميزات.

## قبل بدء أي مهمة

1. اقرأ `docs/PROJECT_CONTEXT.md` و`docs/PROJECT_STATUS.md`.
2. اقرأ أحدث البنود في `docs/DECISIONS.md` و`CHANGELOG.md`.
3. افحص `git status` والفرع الحالي والتغييرات غير الملتزم بها.
4. افحص الكود المرتبط بالمهمة؛ الكود والـmigrations هما دليل الحالة التنفيذية.
5. إذا تعارض التوثيق مع الكود أو Git، تحقق ثم صحح التوثيق.

## قواعد التنفيذ

- حافظ على الوظائف والبيانات السابقة، ولا تستبدل كوداً عاملاً دون بديل مختبر وخطة رجوع.
- لا تخترع أسعاراً أو قواعد عمولة أو صلاحيات أو متطلبات تجارية.
- لا تحذف بيانات تشغيلية أو تاريخ Git، ولا تعيد كتابة تاريخ الفروع المشتركة.
- لا تنفذ `push` أو دمجاً أو تغييراً خارجياً دون إذن صريح.
- لا تضع أسراراً أو `service_role` أو بيانات حقيقية في Git أو في واجهة GitHub Pages.
- افحص التغييرات المحلية واحترمها؛ لا تكتب فوق عمل غير متعلق بالمهمة.
- نفّذ أصغر تغيير آمن، وافصل إعادة الهيكلة عن تغيير السلوك أو البيانات.
- حافظ على RTL، والعربية، والتوافق مع الهاتف والكمبيوتر، واستمرارية GitHub Pages.
- migrations يجب أن تكون قابلة للمراجعة وإعادة إنشاء البيئة، وسياسات RLS جزء من كل تغيير بيانات.
- أي عملية مالية أو ترحيل بيانات تحتاج تحققاً خادمياً، سجل تدقيق، واختبار استرجاع مناسباً للمخاطر.

## الاختبار والتوثيق

- لا تعتبر المهمة مكتملة قبل اختبار مناسب وتسجيل نتيجته.
- حدّث `docs/PROJECT_STATUS.md` في نهاية كل مهمة مهمة.
- سجل القرار التقني أو التجاري الجديد في `docs/DECISIONS.md`.
- أضف التغييرات التي تهم المستخدم أو التشغيل إلى `CHANGELOG.md`.
- حدّث وثائق البنية أو قاعدة البيانات أو الأمان عندما يتغير أي منها.
- اترك تسليماً واضحاً: ما تغير، الملفات المتأثرة، الاختبارات، المخاطر المتبقية، والخطوة التالية.

## عند نقص المعلومات

وثّق السؤال كقرار معلق ولا تفترض الإجابة، خصوصاً في الأسعار، الأدوار، إلغاء الدفعات، اعتماد الأشهر، والاحتفاظ بالسجلات.
